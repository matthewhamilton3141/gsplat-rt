numpy>=1.23.0
opencv-python>=4.8.0
scikit-image>=0.21.0       # marching cubes for TSDF mesh extraction
torch>=2.1.0
torchvision>=0.16.0
transformers>=4.40.0       # Depth Anything V2 via HuggingFace
huggingface_hub>=0.22.0
onnx>=1.14.0
onnxruntime-gpu>=1.16.0,<1.24.0   # 1.2x TensorRT EP links libnvinfer.so.10 (TRT 10)
onnxscript>=0.1.0          # required by torch.onnx.export on torch 2.x
onnxconverter-common>=1.14.0  # fp32→fp16 ONNX for strongly-typed FP16 engine
# TensorRT: install via NVIDIA wheel (requires CUDA toolkit on Linux):
#   pip install "tensorrt>=10,<11" --extra-index-url https://pypi.ngc.nvidia.com
# PIN TO MAJOR 10: must match onnxruntime-gpu's TensorRT EP SONAME
# (onnxruntime-gpu 1.2x -> libnvinfer.so.10). TensorRT 11 (libnvinfer.so.11)
# breaks the EP. Uses IExecutionContext v3 API (>=9.0.0). See scripts/brev_setup.sh.
pyyaml>=6.0
pytest>=7.4.0
