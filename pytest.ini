[pytest]
# The pipeline tests spin up real threads (VideoCapture, Coordinator, TSDFWorker)
# and cross into native code (OpenCV/AVFoundation, pxr). If any of those blocks,
# dump all thread stacks instead of hanging the run forever. 120s is ~6x the
# normal full-suite wall time.
faulthandler_timeout = 120
